name: langchain-agent
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - git
  - pip:
      - fastapi>=0.110
      - uvicorn[standard]>=0.27
      - python-multipart>=0.0.9
      - pydantic>=2.6
      - pydantic-settings>=2.2
      - httpx>=0.26
      - python-dotenv>=1.0
      - langchain
      - langchain-community
      - langchain-milvus
      - langgraph
      - pymilvus>=2.4
      - dashscope
      - sse-starlette
      - pytest
      - ruff
      - black
